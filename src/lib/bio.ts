export type GlassIntensity = "subtle" | "medium" | "heavy" | "ultra";
export type SocialPlatform = "instagram" | "tiktok" | "youtube" | "twitter" | "spotify" | "discord" | "github" | "twitch" | "custom" | "linkedin" | "soundcloud" | "telegram" | "email" | "website";
export type SocialLink = { id: string; platform: SocialPlatform | string; url: string; title?: string; icon_url?: string; active?: boolean };
export type Profile = { id: string; username: string | null; display_name: string | null; bio: string | null; avatar_url: string | null; background_type: "color" | "image" | "video"; background_value: string; card_opacity: number; card_radius: number; card_blur: number; glass_intensity?: GlassIntensity; social_links?: SocialLink[]; accent_color: string; music_url: string | null; music_enabled: boolean; enter_text: string; is_premium: boolean; is_banned: boolean; is_flagged: boolean; views: number; created_at: string; updated_at?: string };
export type BioLink = { id: string; user_id: string; title: string; url: string; position: number; clicks: number; created_at?: string };
export type UserRole = { id: string; user_id: string; role: "admin" | "user" };
export type AuthUser = { id: string; email: string; role?: "admin" | "user"; user_metadata?: { full_name?: string }; created_at?: string };
export type AuthSession = { access_token: string; user: AuthUser };
export type StoredUser = { id: string; email: string; password: string; full_name: string; role: "admin" | "user"; created_at: string };
export const USERNAME_RE = /^[a-z0-9_.]{3,20}$/;
export function normalizeUsername(value: string) { return value.toLowerCase().replace(/[^a-z0-9_.]/g, "").slice(0, 20); }
export function ensureProtocol(url: string) { if (!url) return ""; const trimmed = url.trim(); if (/^(javascript|data|vbscript|file):/i.test(trimmed)) return ""; if (/^https?:\/\//i.test(trimmed)) return trimmed; if (trimmed.startsWith("//")) return `https:${trimmed}`; return `https://${trimmed}`; }
const CSRF_HEADER = { "X-Requested-With": "halo-app" };
type StoreData = { profiles: Profile[]; links: BioLink[]; session: AuthSession | null };
const memoryStore: StoreData = { profiles: [], links: [], session: null };
let isInitialStoreLoaded = false;
type AuthCallback = (event: string, session: AuthSession | null) => void;
const authListeners = new Set<AuthCallback>();
function emitAuth(event: string, session: AuthSession | null) { memoryStore.session = session; authListeners.forEach((cb) => { try { cb(event, session); } catch (e) { console.error("Auth listener error:", e); } }); if (typeof window !== "undefined") window.dispatchEvent(new Event("halo-store-updated")); }
export async function initClientSession(): Promise<AuthSession | null> { if (typeof window === "undefined" || typeof fetch === "undefined") return null; try { const res = await fetch("/api/auth/me", { headers: CSRF_HEADER, credentials: "include" }); if (res.ok) { const data = await res.json(); if (data?.user && data?.session) { const session: AuthSession = { access_token: "server-cookie-session", user: { id: data.user.id, email: data.user.email, role: data.user.role, user_metadata: { full_name: data.user.full_name } } }; memoryStore.session = session; await refreshUserData(); return session; } } } catch (err) { console.warn("Could not fetch server session:", err); } memoryStore.session = null; return null; }
export async function refreshUserData(): Promise<void> { if (typeof window === "undefined" || typeof fetch === "undefined") return; try { const res = await fetch("/api/store", { headers: CSRF_HEADER, credentials: "include" }); if (res.ok) { const data = await res.json(); if (data) { const allProfiles: Profile[] = []; if (data.myProfile) allProfiles.push(data.myProfile); if (Array.isArray(data.publicProfiles)) for (const pub of data.publicProfiles) if (!allProfiles.some((p) => p.id === pub.id)) allProfiles.push(pub); memoryStore.profiles = allProfiles; memoryStore.links = Array.isArray(data.myLinks) ? data.myLinks : []; isInitialStoreLoaded = true; window.dispatchEvent(new Event("halo-store-updated")); } } } catch {} }
if (typeof window !== "undefined") initClientSession().then((session) => emitAuth("INITIAL_SESSION", session));
function getStore(): StoreData { return memoryStore; }
export async function syncToServer(currentUserId?: string): Promise<{ success: boolean; error?: string }> { if (typeof window === "undefined" || typeof fetch === "undefined") return { success: true }; const store = getStore(); const uid = currentUserId || store.session?.user?.id; if (!uid) return { success: false, error: "Authentication required" }; const myProfiles = store.profiles.filter((p) => p.id === uid); const myLinks = store.links.filter((l) => l.user_id === uid); for (const p of myProfiles) { if (p.background_value?.startsWith("data:image/") && p.background_value.length > 250 * 1024) { try { p.background_value = await optimizeImageDataUrl(p.background_value, "background"); } catch {} } if (p.avatar_url?.startsWith("data:image/") && p.avatar_url.length > 60 * 1024) { try { p.avatar_url = await optimizeImageDataUrl(p.avatar_url, "avatar"); } catch {} } } try { const res = await fetch("/api/sync", { method: "POST", headers: { "content-type": "application/json", ...CSRF_HEADER }, credentials: "include", body: JSON.stringify({ profiles: myProfiles, links: myLinks }) }); if (!res.ok) { const errJson = await res.json().catch(() => ({})); return { success: false, error: errJson.error || (res.status === 413 ? "Payload too large (413). Spider Wensors is optimizing your images." : `Sync error (status ${res.status})`) }; } window.dispatchEvent(new Event("halo-store-updated")); try { localStorage.setItem("halo_sync_tick", String(Date.now())); } catch {} return { success: true }; } catch (e) { return { success: false, error: e instanceof Error ? e.message : "Network error syncing data" }; } }
export const auth = { async getSession(): Promise<{ data: { session: AuthSession | null }; error: null }> { if (memoryStore.session) return { data: { session: memoryStore.session }, error: null }; const session = await initClientSession(); return { data: { session }, error: null }; }, onAuthStateChange(callback: AuthCallback) { authListeners.add(callback); callback("INITIAL_SESSION", memoryStore.session); return { data: { subscription: { unsubscribe: () => authListeners.delete(callback) } } }; }, async signUp({ email, password, options }: { email: string; password: string; options?: { data?: { full_name?: string } } }) { try { const res = await fetch("/api/auth/signup", { method: "POST", headers: { "content-type": "application/json", ...CSRF_HEADER }, credentials: "include", body: JSON.stringify({ email, password, full_name: options?.data?.full_name }) }); const json = await res.json(); if (!res.ok || !json.success) return { data: { user: null, session: null }, error: new Error(json.error || "Failed to sign up") }; const user: AuthUser = { id: json.user.id, email: json.user.email, role: json.user.role, user_metadata: { full_name: json.user.full_name } }; const session = { access_token: "server-cookie-session", user }; memoryStore.session = session; await refreshUserData(); emitAuth("SIGNED_IN", session); return { data: { user, session }, error: null }; } catch (err) { return { data: { user: null, session: null }, error: err instanceof Error ? err : new Error("Network error during sign up") }; } }, async signInWithGoogle() { try { const { signInWithPopup, GoogleAuthProvider } = await import("firebase/auth"); const { firebaseAuth } = await import("./firebase"); const result = await signInWithPopup(firebaseAuth, new GoogleAuthProvider()); const idToken = await result.user.getIdToken(); const res = await fetch("/api/auth/google", { method: "POST", headers: { "content-type": "application/json", ...CSRF_HEADER }, credentials: "include", body: JSON.stringify({ idToken }) }); const json = await res.json(); if (!res.ok || !json.success) return { data: { user: null, session: null }, error: new Error(json.error || "Failed to log in with Google") }; const user: AuthUser = { id: json.user.id, email: json.user.email, role: json.user.role, user_metadata: { full_name: json.user.full_name } }; const session = { access_token: "server-cookie-session", user }; memoryStore.session = session; emitAuth("SIGNED_IN", session); return { data: { user, session }, error: null }; } catch (err) { return { data: { user: null, session: null }, error: err instanceof Error ? err : new Error("Failed to authenticate with Google") }; } }, async signInWithPassword({ email, password }: { email: string; password: string }) { try { const res = await fetch("/api/auth/login", { method: "POST", headers: { "content-type": "application/json", ...CSRF_HEADER }, credentials: "include", body: JSON.stringify({ email, password }) }); const json = await res.json(); if (!res.ok || !json.success) return { data: { user: null, session: null }, error: new Error(json.error || "Failed to log in") }; const user: AuthUser = { id: json.user.id, email: json.user.email, role: json.user.role, user_metadata: { full_name: json.user.full_name } }; const session = { access_token: "server-cookie-session", user }; memoryStore.session = session; await refreshUserData(); emitAuth("SIGNED_IN", session); return { data: { user, session }, error: null }; } catch (err) { return { data: { user: null, session: null }, error: err instanceof Error ? err : new Error("Network error during sign in") }; } }, async signOut() { try { await fetch("/api/auth/logout", { method: "POST", headers: CSRF_HEADER, credentials: "include" }); } catch {} memoryStore.session = null; memoryStore.profiles = []; memoryStore.links = []; emitAuth("SIGNED_OUT", null); return { error: null }; }, async changePassword(currentPassword: string, newPassword: string) { try { const res = await fetch("/api/change-password", { method: "POST", headers: { "content-type": "application/json", ...CSRF_HEADER }, credentials: "include", body: JSON.stringify({ currentPassword, newPassword }) }); const json = await res.json(); return res.ok ? { error: null } : { error: new Error(json.error || "Failed to update password") }; } catch (err) { return { error: err instanceof Error ? err : new Error("Failed to change password") }; } } };
type QueryFilter = { column: string; operator: "eq" | "ilike"; value: any };
type OrderConfig = { column: string; ascending: boolean };
class ManualQueryBuilder<T extends Record<string, unknown>> { private tableName: "profiles" | "links" | "user_roles"; private filters: QueryFilter[] = []; private orderConfig: OrderConfig | null = null; private limitCount: number | null = null; private isSingle = false; private isMaybeSingle = false; private operation: "select" | "update" | "delete" | "insert" = "select"; private payload: any = null; constructor(tableName: "profiles" | "links" | "user_roles") { this.tableName = tableName; } select(_columns?: string) { if (this.operation !== "insert" && this.operation !== "update") this.operation = "select"; return this; } eq(column: string, value: any) { this.filters.push({ column, operator: "eq", value }); return this; } ilike(column: string, value: any) { this.filters.push({ column, operator: "ilike", value }); return this; } order(column: string, config: { ascending?: boolean } = {}) { this.orderConfig = { column, ascending: config.ascending ?? true }; return this; } limit(count: number) { this.limitCount = count; return this; } single() { this.isSingle = true; return this; } maybeSingle() { this.isMaybeSingle = true; return this; } update(changes: Partial<T>) { this.operation = "update"; this.payload = changes; return this; } insert(values: Partial<T>) { this.operation = "insert"; this.payload = values; return this; } delete() { this.operation = "delete"; return this; } private applyFilters<R extends Record<string, unknown>>(items: R[]): R[] { return items.filter((item) => this.filters.every((f) => { const val = item[f.column]; if (f.operator === "eq") return val === f.value; if (f.operator === "ilike") return typeof val === "string" && typeof f.value === "string" ? val.toLowerCase() === f.value.toLowerCase() : val === f.value; return true; })); } async execute(): Promise<{ data: any; error: Error | null }> { const store = getStore(); const currentUser = store.session?.user; if (this.tableName === "user_roles") { let list: UserRole[] = currentUser ? [{ id: "role-" + currentUser.id, user_id: currentUser.id, role: currentUser.role || "user" }] : []; list = this.applyFilters(list); return { data: this.isSingle || this.isMaybeSingle ? list[0] ?? null : list, error: null }; } if (this.tableName === "profiles") { if (this.operation === "select") { let list = [...store.profiles]; list = this.applyFilters(list); if (this.orderConfig) { const { column, ascending } = this.orderConfig; list.sort((a, b) => { const av = a[column as keyof Profile]; const bv = b[column as keyof Profile]; if (av == null) return 1; if (bv == null) return -1; if (av < bv) return ascending ? -1 : 1; if (av > bv) return ascending ? 1 : -1; return 0; }); } if (this.limitCount != null) list = list.slice(0, this.limitCount); return { data: this.isSingle || this.isMaybeSingle ? list[0] ?? null : list, error: null }; } if (this.operation === "update") { const idFilter = this.filters.find((f) => f.column === "id" && f.operator === "eq"); const targetId = idFilter ? String(idFilter.value) : undefined; let updatedCount = 0; store.profiles = store.profiles.map((p) => { const match = this.filters.every((f) => p[f.column as keyof Profile] === f.value); if (match) { updatedCount++; return { ...p, ...this.payload, updated_at: new Date().toISOString() }; } return p; }); if (updatedCount === 0 && targetId) { const existingIdx = store.profiles.findIndex((p) => p.id === targetId); if (existingIdx >= 0) { store.profiles[existingIdx] = { ...store.profiles[existingIdx], ...this.payload, updated_at: new Date().toISOString() }; updatedCount = 1; } } const syncRes = await syncToServer(currentUser?.id); if (!syncRes.success) return { data: null, error: new Error(syncRes.error) }; return { data: { count: updatedCount }, error: null }; } if (this.operation === "delete") { store.profiles = store.profiles.filter((p) => !this.filters.every((f) => p[f.column as keyof Profile] === f.value)); const syncRes = await syncToServer(currentUser?.id); return syncRes.success ? { data: null, error: null } : { data: null, error: new Error(syncRes.error) }; } } if (this.tableName === "links") { if (this.operation === "select") { let list = this.applyFilters([...store.links]); if (this.orderConfig) { const { column, ascending } = this.orderConfig; list.sort((a, b) => { const av = a[column as keyof BioLink]; const bv = b[column as keyof BioLink]; if (av == null) return 1; if (bv == null) return -1; if (av < bv) return ascending ? -1 : 1; if (av > bv) return ascending ? 1 : -1; return 0; }); } return { data: this.isSingle || this.isMaybeSingle ? list[0] ?? null : list, error: null }; } if (this.operation === "insert") { const newLink: BioLink = { id: "lnk-" + Math.random().toString(36).slice(2, 10), user_id: this.payload.user_id || currentUser?.id || "", title: this.payload.title || "New Link", url: this.payload.url || "https://", position: this.payload.position ?? store.links.length, clicks: 0, created_at: new Date().toISOString(), ...this.payload }; store.links.push(newLink); const syncRes = await syncToServer(currentUser?.id); return syncRes.success ? { data: newLink, error: null } : { data: null, error: new Error(syncRes.error) }; } if (this.operation === "update") { store.links = store.links.map((link) => this.filters.every((f) => link[f.column as keyof BioLink] === f.value) ? { ...link, ...this.payload } : link); const syncRes = await syncToServer(currentUser?.id); return syncRes.success ? { data: { count: 1 }, error: null } : { data: null, error: new Error(syncRes.error) }; } if (this.operation === "delete") { store.links = store.links.filter((link) => !this.filters.every((f) => link[f.column as keyof BioLink] === f.value)); const syncRes = await syncToServer(currentUser?.id); return syncRes.success ? { data: null, error: null } : { data: null, error: new Error(syncRes.error) }; } } return { data: null, error: null }; } then<TResult1 = { data: unknown; error: Error | null }, TResult2 = never>(onfulfilled?: ((value: { data: unknown; error: Error | null }) => TResult1 | PromiseLike<TResult1>) | null, onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null): Promise<TResult1 | TResult2> { return this.execute().then(onfulfilled, onrejected); } }
export const db = { from(tableName: "profiles" | "links" | "user_roles") { return new ManualQueryBuilder(tableName); }, async rpc(fnName: string, args: Record<string, unknown> = {}) { const store = getStore(); if (fnName === "username_available") { const username = String(args["_username"] || "").toLowerCase().trim(); const userId = args["_user_id"] ? String(args["_user_id"]) : undefined; if (!username || username.length < 3 || username.length > 20) return { data: false, error: null }; try { const q = new URLSearchParams({ username }); if (userId) q.set("userId", userId); const res = await fetch(`/api/username-available?${q}`); if (res.ok) { const json = await res.json(); if (typeof json.available === "boolean") return { data: json.available, error: null }; } } catch {} const localConflict = store.profiles.some((p) => p.username?.toLowerCase() === username && p.id !== userId); return { data: !localConflict, error: null }; } if (fnName === "claim_username") { const username = String(args["_username"] || "").toLowerCase().trim(); const userId = String(args["_user_id"] || ""); try { const res = await fetch("/api/claim", { method: "POST", headers: { "content-type": "application/json", ...CSRF_HEADER }, credentials: "include", body: JSON.stringify({ userId, username }) }); const json = await res.json(); if (!res.ok || !json.success) return { data: null, error: new Error(json.error || `The username "${username}" is already reserved by another user.`) }; } catch (err) { return { data: null, error: err instanceof Error ? err : new Error("Network error") }; } store.profiles = store.profiles.map((p) => p.id === userId ? { ...p, username, display_name: p.display_name || username, updated_at: new Date().toISOString() } : p); return { data: { success: true, username }, error: null }; } if (fnName === "increment_profile_view") { const username = String(args["_username"] || "").toLowerCase().trim(); try { const res = await fetch("/api/view", { method: "POST", headers: { "content-type": "application/json", ...CSRF_HEADER }, body: JSON.stringify({ username }) }); const json = await res.json(); if (typeof json.views === "number") return { data: json, error: null }; } catch {} return { data: null, error: null }; } if (fnName === "increment_link_click") { fetch("/api/click", { method: "POST", headers: { "content-type": "application/json", ...CSRF_HEADER }, body: JSON.stringify({ linkId: String(args["_link_id"] || "") }) }).catch(() => {}); return { data: null, error: null }; } if (fnName === "platform_stats") { try { const res = await fetch("/api/stats"); if (res.ok) return { data: [await res.json()], error: null }; } catch {} return { data: [{ total_profiles: store.profiles.length, active_profiles: store.profiles.filter((p) => p.username && !p.is_banned).length, total_views: store.profiles.reduce((a, b) => a + (b.views || 0), 0), total_clicks: store.links.reduce((a, b) => a + (b.clicks || 0), 0), total_links: store.links.length }], error: null }; } return { data: null, error: null }; } };
export async function optimizeImageDataUrl(input: string | File, folder: "avatar" | "background" | string = "background"): Promise<string> { if (typeof window === "undefined") return typeof input === "string" ? input : readDirect(input); let objectUrl = ""; let isObjectUrl = false; if (typeof input === "string") { if (!input.startsWith("data:image/")) return input; const threshold = folder === "avatar" ? 50 * 1024 : 200 * 1024; if (input.length < threshold) return input; objectUrl = input; } else { if (!input.type.startsWith("image/")) return readDirect(input); if (input.type === "image/svg+xml" || input.type === "image/gif") return readDirect(input); objectUrl = URL.createObjectURL(input); isObjectUrl = true; } try { return await new Promise<string>((resolve) => { const img = new Image(); img.onload = () => { if (isObjectUrl) try { URL.revokeObjectURL(objectUrl); } catch {} let width = img.naturalWidth || img.width, height = img.naturalHeight || img.height; if (!width || !height) return resolve(typeof input === "string" ? input : ""); const maxDim = folder === "avatar" ? 320 : 1080; if (width > maxDim || height > maxDim) { if (width > height) { height = Math.round(height * maxDim / width); width = maxDim; } else { width = Math.round(width * maxDim / height); height = maxDim; } } const canvas = document.createElement("canvas"); canvas.width = Math.max(width, 1); canvas.height = Math.max(height, 1); const ctx = canvas.getContext("2d"); if (!ctx) return resolve(typeof input === "string" ? input : ""); ctx.drawImage(img, 0, 0, width, height); let result = canvas.toDataURL("image/jpeg", folder === "avatar" ? 0.82 : 0.78); const maxChars = folder === "avatar" ? 75 * 1024 : 360 * 1024; if (result.length > maxChars) result = canvas.toDataURL("image/jpeg", folder === "avatar" ? 0.7 : 0.68); if (result.length > maxChars && folder !== "avatar") { const c = document.createElement("canvas"); c.width = Math.max(Math.round(width * 0.75), 1); c.height = Math.max(Math.round(height * 0.75), 1); const x = c.getContext("2d"); if (x) { x.drawImage(canvas, 0, 0, c.width, c.height); result = c.toDataURL("image/jpeg", 0.65); } } resolve(result); }; img.onerror = () => resolve(typeof input === "string" ? input : ""); img.src = objectUrl; }); } catch { return typeof input === "string" ? input : readDirect(input); } }
export async function uploadMedia(userId: string, file: File, folder: string, onProgress?: (progress: number) => void): Promise<string> { if (file.type.startsWith("video/") && file.size > 1 * 1024 * 1024) throw new Error("Uploaded video clip exceeds 1MB. Please select a video file under 1MB."); if (file.type.startsWith("audio/") && file.size > 15 * 1024 * 1024) throw new Error("Audio file exceeds 15MB. For full songs, please paste a direct audio URL."); try { const formData = new FormData(); formData.append("file", file); formData.append("folder", folder); formData.append("userId", userId); const url = await new Promise<string>((resolve, reject) => { const xhr = new XMLHttpRequest(); xhr.open("POST", "/api/upload"); xhr.setRequestHeader("x-requested-with", "halo-app"); if (onProgress && xhr.upload) xhr.upload.onprogress = (e) => { if (e.lengthComputable) onProgress(Math.round(e.loaded * 100 / e.total)); }; xhr.onload = () => { if (xhr.status >= 200 && xhr.status < 300) { try { const data = JSON.parse(xhr.responseText); data.url ? resolve(data.url) : reject(new Error(data.error || "Upload failed")); } catch { reject(new Error("Invalid JSON response from upload API")); } } else reject(new Error(`Server returned status ${xhr.status}`)); }; xhr.onerror = () => reject(new Error("Network error during upload")); xhr.send(formData); }); if (url) return url; } catch (err) { console.warn("Direct server upload failed, falling back:", err); } if (file.type.startsWith("image/") && typeof window !== "undefined") return optimizeImageDataUrl(file, folder); return readDirect(file); }
function readDirect(file: File): Promise<string> { return new Promise((resolve, reject) => { const reader = new FileReader(); reader.onload = () => resolve(reader.result as string); reader.onerror = () => reject(new Error("Failed to read file")); reader.readAsDataURL(file); }); }
export async function fetchProfileByUsername(username: string) { try { const cleanUser = username.toLowerCase().trim(); if (typeof window === "undefined") { const globalStore = (globalThis as any).__HALO_SERVER_STORE__; if (globalStore?.getProfile) { const found = await globalStore.getProfile(cleanUser); if (found) return found; } } if (typeof window !== "undefined" && typeof fetch !== "undefined") { try { const res = await fetch(`/api/profile/${encodeURIComponent(cleanUser)}?t=${Date.now()}`, { cache: "no-store" }); if (res.ok) { const data = await res.json(); if (data?.profile) { const pIdx = memoryStore.profiles.findIndex((p) => p.id === data.profile.id); if (pIdx >= 0) memoryStore.profiles[pIdx] = data.profile; else memoryStore.profiles.push(data.profile); if (Array.isArray(data.links)) memoryStore.links = memoryStore.links.filter((l) => l.user_id !== data.profile.id).concat(data.links); return { profile: data.profile, links: data.links || [] }; } } } catch {} } const profile = memoryStore.profiles.find((p) => p.username?.toLowerCase() === cleanUser); if (!profile) return null; return { profile, links: memoryStore.links.filter((l) => l.user_id === profile.id).sort((a, b) => a.position - b.position) }; } catch (e) { console.warn("fetchProfileByUsername failed:", e); return null; } }
